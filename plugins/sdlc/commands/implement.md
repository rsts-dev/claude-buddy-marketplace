---
description: Implement step (I of R-PIV) — execute strictly from an approved plan in a fresh context, delegating to buddy's TDD implementation when tasks exist.
argument-hint: "[spec slug | path to plan.md]"
---

Read and execute the Implement skill at `skills/Implement/SKILL.md`.

**User provided input**: $ARGUMENTS

Build only what the approved plan specifies. If it drifts, stop and return to planning.
