---
description: Root-cause a bug or failure, then propose the rule or context module that would have prevented it — every bug improves the AI layer.
argument-hint: "[description of the bug / failing behavior]"
---

Read and execute the Rca skill at `skills/Rca/SKILL.md`.

**User provided input**: $ARGUMENTS

The output is not just a fix — it is a proposed change to CLAUDE.md or .claude/context/ so the system does not repeat the mistake.
