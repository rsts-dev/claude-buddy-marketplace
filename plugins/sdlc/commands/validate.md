---
description: Validate step (V of R-PIV) — run the project's tests, linting, type-checking, and build, then report pass/fail with actionable output.
argument-hint: "[optional: specific level e.g. tests|lint|types|build]"
---

Read and execute the Validate skill at `skills/Validate/SKILL.md`.

**User provided input**: $ARGUMENTS

Discover validation commands from CLAUDE.md / the foundation. Never claim success without running them.
