---
description: Bootstrap the AI layer on a brownfield repo — run buddy Foundation, then generate a lean CLAUDE.md, .claude/context/ modules, and an .mcp.json scaffold.
argument-hint: "[optional notes or focus areas]"
---

Read and execute the Init skill at `skills/Init/SKILL.md`.

Init delegates the foundation to buddy (`/buddy:foundation` → `/directive/foundation.md`), then layers the AI-native pieces on top.

**User provided input**: $ARGUMENTS
