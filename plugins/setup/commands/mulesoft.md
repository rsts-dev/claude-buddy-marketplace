---
description: Install the official MuleSoft API-design skills (api-spec-validator, jtbd-*, api-doc-generator, …) by driving their upstream install. Anypoint CLI prerequisite.
---

Read and execute the MuleSoftSetup skill at `skills/MuleSoftSetup/SKILL.md`.

**User provided input**: $ARGUMENTS

The skill fetches the upstream install instructions at run time and drives the official `/plugin marketplace add` with permission. Nothing is vendored.
