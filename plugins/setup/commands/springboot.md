---
description: Install Julien Dubois' Dr JSkill Spring Boot 4.x project generator (jdubois/dr-jskill) into your skills directory. Java 25 / Node 24 / Docker prerequisites.
---

Read and execute the SpringBootSetup skill at `skills/SpringBootSetup/SKILL.md`.

**User provided input**: $ARGUMENTS

The skill fetches the upstream install instructions at run time and drives the official install (clone into the skills directory) with permission. Nothing is vendored.
