# Reference Materials — Default Domain

The default domain has no domain-specific reference materials. It relies on codebase analysis and foundation principles to guide development.

For domain-specific guidance, ensure the correct domain is detected during foundation creation. Available domains can be found in sibling directories.
