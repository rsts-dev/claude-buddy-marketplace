---
description: Create professional git commits with conventional commit format and clean commit rules.
---

Read and execute the SourceControl skill at `skills/SourceControl/SKILL.md`.

Route to the **Commit** workflow.

**User provided input**: $ARGUMENTS

Parse arguments for interaction mode (`--yes`/`-y`, `--interactive`/`-i`), ticket references (SDO-123, #10), and any other flags.
